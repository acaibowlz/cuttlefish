+++
title = "About"
slug = "about"
+++

This is a standalone **page**. It belongs to no content-type index and no
taxonomy — it just exists at its permalink (`/about/`).

Pages are perfect for things like an about page, a colophon, or a contact page.
Add more under `content/pages/`.
